-- ============================================================
-- V5: poem_tags 性能优化索引
-- 为 tag_name 添加单独索引，加速按标签名检索诗词
-- ============================================================

ALTER TABLE poem_tags ADD INDEX idx_poem_tags_tag (tag_name);